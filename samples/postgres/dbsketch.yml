provider: postgres
connectionString: ${DBSKETCH_POSTGRES_CONNECTION}

include:
  tables:
    - "public.*"

output:
  path: samples/output/schema.dot
  format: raw

diagram:
  renderer: dot
  direction: LR
