provider: postgres
connectionString: ${DBSKETCH_POSTGRES_CONNECTION}

defaults:
  output:
    format: raw
  diagram:
    renderer: dot
    direction: LR

diagrams:
  - name: public
    include:
      tables:
        - "public.*"
    output:
      path: samples/output/postgres-public.dot
