provider: mysql
connectionString: "${DBSKETCH_MYSQL_CONNECTION}"

defaults:
  output:
    format: raw
  diagram:
    renderer: dot
    direction: LR

diagrams:
  - name: full
    output:
      path: samples/output/mysql-full.dot
